server:
  port: 8082

rabbitmq:
  queue: message.queue
  host: localhost
  port: 5672
  username: guest
  password: guest
  queue_with_auto_ack:
    name: queue_with_auto_ack
  queue_without_auto_ack:
    name: queue_without_auto_ack